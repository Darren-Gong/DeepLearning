torch>=2.0
torchvision>=0.15
transformers>=4.38
diffusers>=0.27
accelerate>=0.25
safetensors>=0.4
pillow>=10.0
numpy>=1.24
opencv-python>=4.8
tqdm>=4.66
# Segment Anything (official). If network/git is unavailable, clone locally and do:
#   pip install -e /path/to/segment-anything
git+https://github.com/facebookresearch/segment-anything.git
